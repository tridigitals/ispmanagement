name: isp_monorepo
description: ISP Management monorepo - desktop (Tauri), mobile apps (Flutter), shared packages
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.22.0"

workspace:
  - apps/mobile-customer
  - packages/api-client
  - packages/ui-kit
  - packages/config

dev_dependencies:
  melos: ^6.2.0
  very_good_analysis: ^6.0.0

dependency_overrides:
  package_info_plus: ^8.0.0
