<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cache directory for temporary camera capture files -->
    <cache-path
        name="cache"
        path="." />
    <!-- External files (avatars) directory -->
    <external-files-path
        name="external_files"
        path="." />
    <!-- Internal files directory for app private files -->
    <files-path
        name="files"
        path="." />
</paths>
