# Static client definitions are intentionally minimal here.
# Active router/NAS clients are loaded from PostgreSQL through the SQL module
# (`read_clients = yes`). Keep this file only for local debugging if needed.
